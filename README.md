# esbuild-tsc

quickly build `typescript` with `esbuild`

# Features

* fast 
* support path alias

# How to use

```bash
etsc 
```


## emitDecoratorMetadata 

add `// transpileModule = On` to top of file 